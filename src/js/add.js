import React, { PropTypes } from 'react'

class Add extends React.Component {
  render () {
    return (
      <input type="text" placeholder="See what you can do..."/>
    )
  }
}

export default Add;
